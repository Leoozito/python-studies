# existe algo chamado "dicionario" no python, a estrutura abaixo:
## conta = {"numero": 123, "titular":"Nico"} -> que consegue juntar variaveis em uma coisa só
### e que você consegue puxar informação -> conta["numero"]

# Um objeto é uma instância de uma classe onde podemos definir valores para seus atributos.
# Python ao criar objetos, pode executar uma função automaticamente, dentro da nossa classe para definirmos os atributos

# Uma boa analogia é considerar a classe como a receita para a criação de por exemplo, um delicioso bolo de cenoura
# -----------------------------------------------------------------------------------------
    # quando deixamos alguns atributos privado, a forma de acesso deles muda
    # conta3._ContaCorrente__saldo
    
# sempre devemos estruturar bem o nosso código, exemplo é as classes que tem que ter funções que estejam de acordo com o que a classe foi intitulada, para que seja responsavel por apenas uma coisa

# Para fazer um metodo estatico precisa configurar para n ter o @property, pq o property sempre precisa do self onde fica os objetos.
# Métodos estáticos devem ser usados com cautela caso contrário pode-se fugir do paradigma da orientação a objetos.